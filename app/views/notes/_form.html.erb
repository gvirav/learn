<%= simple_form_for([@checkpoint, @note], :html => {:class=>'form-horizontal'}) do |f| %><br/>
	<% if @note.errors.any? %>
	<div id="error_explanation">
		<h3><%= pluralize(@note.errors.count, "error") %> prohibited this from saving:</h3>
			<ul>
				<%= @note.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
				<% end %>
			</ul>
		<% end %>
	</div>
	<%= f.text_field :url, :placeholder => "Relevent URL for Note" %><br/>
	<%= f.text_area :body, :placeholder => "Add Checkpoint Note" %><br/>
	<%= f.button :submit %><br/>
<% end %>

